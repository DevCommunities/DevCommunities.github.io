# Optional analytics IDs. Tracking loads only after visitor consent.
PUBLIC_GA_ID=
PUBLIC_META_PIXEL_ID=
# Set SITE_URL in the build environment to generate canonical URLs and sitemap.
# SITE_URL=https://your-domain.example
